name: dart_versioner
description: A utility to auto-increment a pubspec.yaml, similar to "npm version"
version: 1.0.0

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  pub_semver: ^2.1.4
  pubspec_parse: ^1.2.3

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
